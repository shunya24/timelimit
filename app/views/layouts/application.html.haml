!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title TimeLimit
    = csrf_meta_tags
    = csp_meta_tag
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload'
    = javascript_pack_tag 'application', 'data-turbolinks-track': 'reload'
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}/
    %link{:crossorigin => "anonymous", :href => "https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css", :rel => "stylesheet"}/
  %body
    %header.header
      %p.header_pageTitle
        = link_to 'Time Limit', root_path
        = link_to '買い物リスト', shoppings_path
        ゴミ箱
      -if user_signed_in?
        .dropdown
          = image_tag 'images.png', class: 'header_avatar dropBtn'
          .dropdown_content
            = link_to 'プロフィール'
            = link_to 'ログアウト', destroy_user_session_path, data: { method: 'delete', confirm: '本当にログアウトしますか？' }
      - else
        = link_to 'ログイン', new_user_session_path, class: 'header_loginBtn'
    - if flash.present?
      .flash
        - flash.each do |key, value|
          %div{:class => key}= value
    = yield
